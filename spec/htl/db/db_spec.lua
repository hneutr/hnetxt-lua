local Path = require("hl.Path")

local db

-- before_each(function()
--     db = require("htl.db")
--     db.before_test()
-- end)

-- after_each(function()
--     db.after_test()
-- end)

describe("it", function()
    it("works", function()
        -- db = require("htl.db")
        -- print(db.blah)
        -- db = nil
    end)
end)
