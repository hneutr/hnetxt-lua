require("htl")

local M = require("htl.Snippet")

describe("testing", function()
    local M = M.M
    it("works", function()
        -- local s = M("quote", {source = "hunter", ["on page"] = "steve"})
        -- local s = M("quote", {source = "hunter"})
    end)
end)
